{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "typeRoots": ["node_modules/@types", "src/types"],
    "types": ["google.maps"]
  },
  "include": ["."],
  "exclude": [
    "node_modules",
    "cypress",
    "**/*.e2e.ts",
    "src/tests",
    "**/*.test.*",
    "jest.*",
    "contentful-migrations"
  ]
}
